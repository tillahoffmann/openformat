from setuptools import find_packages, setup

setup(
    name='openformat',
    packages=find_packages(),
    install_requires=[
        'numpy>=1.3'
    ],
    version="0.1",
    author="Till Hoffmann",
)
